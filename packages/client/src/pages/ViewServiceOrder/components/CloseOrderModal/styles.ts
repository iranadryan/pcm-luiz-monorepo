import styled from 'styled-components';

export const Container = styled.div`
  .close-button {
    display: flex;
    align-items: center;
    background: transparent;
    border: none;
    margin-left: auto;
  }

  .date-input {
    margin-top: 8px;
    display: flex;
    gap: 8px;
  }
`;
