import styled from 'styled-components';

export const Container = styled.div`
  .close-button {
    display: flex;
    align-items: center;
    background: transparent;
    border: none;
    margin-left: auto;
  }

  .qty-input {
    margin-top: 4px;
    display: flex;
    align-items: center;
    gap: 8px;

    span {
      margin-top: 20px;
    }
  }
`;
